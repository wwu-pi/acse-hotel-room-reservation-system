CREATE TABLE Client(
    ClientNo INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    FirstName VARCHAR(20) NOT NULL,
    LastName VARCHAR (20) NOT NULL,
    PhoneNo VARCHAR(20));
CREATE TABLE Room(
   RoomNo INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
   Size CHAR(6) NOT NULL,
   Price INT NOT NULL);
CREATE TABLE Reservation(
   Arrival DATE NOT NULL,
   Departure DATE NOT NULL,
   ClientNo INT NOT NULL,
   RoomNo INT NOT NULL);
 INSERT INTO Room VALUES(NULL,'Single',50);
 INSERT INTO Room VALUES(NULL,'Double',69);
 INSERT INTO Client VALUES(NULL,'Willy','Winzig','0170 123456');
 
   